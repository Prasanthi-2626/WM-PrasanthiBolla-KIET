1. Bin senses fill level only when change is detected
2. Data is sent to zone gateway via MQTT
3. Zone gateway computes Zone Health Score
4. Cloud triggers route optimization if threshold crossed
5. Trucks are dispatched only when necessary
