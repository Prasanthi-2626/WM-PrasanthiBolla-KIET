Zone gateways aggregate bin data locally.
If cloud connectivity fails, gateways cache events
and replay them when the network is restored.

This enables graceful degradation instead of total failure.
